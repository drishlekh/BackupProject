<!-- res/layout/activity_main.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_marginTop="30dp">

    <Button
        android:id="@+id/btn_play_from_storage"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginBottom="30dp"
        android:foregroundGravity="center"
        android:gravity="center"
        android:text="Play from Device Storage" />

    <Button
        android:id="@+id/btn_play_live_microphone"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginBottom="30dp"
        android:backgroundTint="@color/black"
        android:foregroundGravity="center"
        android:gravity="center"
        android:text="Play Live from Microphone" />

    <Button
        android:id="@+id/btn_play_from_youtube"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginBottom="30dp"
        android:backgroundTint="@color/design_default_color_error"
        android:foregroundGravity="center"
        android:gravity="center"
        android:text="Play from YouTube Video" />
</LinearLayout>
